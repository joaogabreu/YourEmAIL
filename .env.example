# Variáveis de ambiente (.env raiz)

# Backend
GEMINI_API_KEY=coloque_sua_chave_aqui
# Modelo Gemini (gemini-1.5-flash foi descontinuado)
GEMINI_MODEL=gemini-2.5-flash

# CORS (separado por vírgula).
CORS_ALLOWED_ORIGINS=http://localhost:5173

# Porta e debug (opcionais)
PORT=5000
FLASK_DEBUG=true

# Banco de dados (Postgres via Docker)
POSTGRES_USER=youremail
POSTGRES_PASSWORD=youremail
POSTGRES_DB=youremail
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
# Ou use URL completa (tem prioridade sobre POSTGRES_*):
# DATABASE_URL=postgresql://youremail:youremail@localhost:5432/youremail

# Frontend (Vite)
# URL do backend consumida pelo frontend
VITE_BACKEND_URL=http://127.0.0.1:5000

# Google APIs (Frontend)
VITE_GOOGLE_CLIENT_ID=seu_client_id_do_google_aqui
VITE_GOOGLE_API_KEY=sua_api_key_do_google_aqui

# Guia rápido:
# 1. Acesse https://console.cloud.google.com/
# 2. Ative Gmail API
# 3. Crie credenciais:
#    - API Key (para VITE_GOOGLE_API_KEY)
#    - OAuth 2.0 Client ID (para VITE_GOOGLE_CLIENT_ID)
# 4. Configure o URI de redirecionamento: http://localhost:5173
# 5. Para o Gemini, acesse https://makersuite.google.com/ e crie uma API Key
